// made variable 
const Locations = (locations) => {
    return `
<section class="location">${locations.location}</section>
<section class="quote">${locations.quote}</section>
`

}

export default Locations